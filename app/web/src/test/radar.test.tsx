import{render,screen}from'@testing-library/react';import{describe,it,expect}from'vitest';import{Radar}from'../components/Radar';describe('Radar',()=>{it('renderiza nomes e polígono dos dados',()=>{render(<Radar items={[{id:'arrays',name:'Arrays',group:'sequence',score:50,prerequisites:[]},{id:'hashing',name:'Hashing',group:'sequence',score:20,prerequisites:[]}]}/>);expect(screen.getByText('Arrays')).toBeInTheDocument();expect(screen.getByTestId('score-polygon')).toHaveAttribute('points')})});
